# Sigma Motors Market Signal restyle

User linked an existing staging workbook (urlId `1tAWeYDFblZOEGu9Gq4WjW`,
org slug papercranestaging) and three high-fidelity mockups
(GA Motors / Volterra / Horizon Motors).
Claude's first pass looked like a default Sigma report. Goal: make it read
like those mockups.

## Constraints discovered this session

- Injected `SIGMA_CLIENT_ID` / `SIGMA_CLIENT_SECRET` authenticate as
  Maximus Redman on **sigma-on-sigma**, not papercranestaging. The linked
  workbook is not visible to this token (10k+ files scanned, urlId miss).
- Therefore this build publishes a new workbook in Maximus's home folder
  on sigma-on-sigma, using the Sigma Sample Database connection (the only
  Snowflake conn that resolved custom SQL here).
- Visual target is the shared mockup composition, not a v2 company-dashboard
  reskin: navy chrome, white KPI cards, tinted AI band, quick-questions
  rail, 5 regional donuts, ranked bar, trend.

## Layout

```
navy header (wordmark + pill nav + date)
title + region filter
5 white KPI cards (icon, value, delta, sparkline)
tinted AI insight + Explore Scenarios
Quick Questions + chat | Regional Demand Pulse donuts
                       | ranked EV bar | backlog trend
```
